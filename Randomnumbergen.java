package sample;

public class Randomnumbergen {
    for(int i =0; i<3; i++){
        int randomInt = (int)(10.0 * Math.random());
        System.out.println(" random int between 1 and 10 : " + randomInt );
    }

    Output:
    random int between 1 and 10 : 4
    random int between 1 and 10 : 0
    random int between 1 and 10 : 2
}
